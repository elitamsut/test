# Default values for flask-hello-world.
# This is a YAML-formatted file.
# Declare new variables to be passed into your templates.

replicaCount: 1

image:
  repository: elitamsut/myapp
  pullPolicy: Always
  tag: "v1.0.18"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 8081

ingress:
  enabled: true
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: customer2-app-127-0-0-1.nip.io
      paths:
        - /
  tls: []

resources:
  requests:
    memory: "256Mi"  # Adjust as necessary for your application
    cpu: "500m"      # Adjust as necessary for your application
  limits:
    memory: "512Mi"  # Adjust as necessary for your application
    cpu: "1"         # Adjust as necessary for your application

nodeSelector: {}

tolerations: []

affinity: {}

livenessProbe:
  httpGet:
    path: /
    port: 8081  # Ensure this matches
  initialDelaySeconds: 30
  periodSeconds: 10
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /
    port: 8081  # Ensure this matches
  initialDelaySeconds: 10
  periodSeconds: 5
  failureThreshold: 3
